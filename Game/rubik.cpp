#include <iostream>
#include "rubik.h"

rubik::rubik(int row, Scene* scene) {
//    int indx = 0;
//    for (int i = -(row / 2); i <= row / 2; i++){
//        x.insert({i, vector<block*>()});
//        y.insert({i, vector<block*>()});
//        z.insert({i, vector<block*>()});
//    }
//    for (int i = -(row / 2); i <= row / 2; i++)
//        for (int j = -(row / 2); j <= row / 2; j++)
//            for (int k = -(row / 2); k <= row / 2; k++) {
//                auto *b = new block(<#initializer#>, 0, indx, glm::vec3(i, j, k));
//                x.at(i).push_back(b);
//                y.at(j).push_back(b);
//                z.at(k).push_back(b);
//                indx ++;
//            }
}
